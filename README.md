# hello_prototype
This project is a simplified prototype based on the structure of  boam netconf repo, demonstrating a basic C++ application with separate unit tests (UT) and system component tests (SCT).
